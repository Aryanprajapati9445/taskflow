version: '3.8'

services:
  backend:
    build: ./backend
    container_name: taskflow-api
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGO_URI=mongodb+srv://aryanprajapati9582_db_user:GfHoho0bNXXH9AOL@cluster0.wdaoh3j.mongodb.net/taskmanager?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=change_this_to_a_64_char_secret_in_production_please_its_important
      - JWT_ACCESS_EXPIRY=15m
      - JWT_REFRESH_EXPIRY=7d
      - CLIENT_URL=http://localhost
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: taskflow-ui
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
